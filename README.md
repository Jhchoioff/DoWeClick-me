# DoWeClick-me
A free daily matching and compatibility experience for two real people.

Front-end prototype with birthday entry and a shareable invitation to the input form. Daily compatibility calculations and shared results are not implemented. Birthdays remain in page memory and are not sent or saved.

Run `npm run dev` for development, `npm run build` for a production build, and `npm run lint` for lint checks.
